# The conformance vector generator (Rust, not published)
vectors-gen/target
**/*.rs.bk

# generated conformance fixture, regenerated from vectors-gen
vectors-gen/vectors.json

# Composer dependencies (phpunit/phpunit). composer.lock stays committed,
# same reasoning as the two Cargo.lock files: a reproducible install.
vendor
.phpunit.result.cache
